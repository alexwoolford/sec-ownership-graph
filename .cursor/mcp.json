{
  "mcpServers": {
    "secgraph-ownership": {
      "command": "${workspaceFolder}/scripts/run_ownership_mcp.sh",
      "args": ["--database", "secgraph"],
      "envFile": "${workspaceFolder}/.env"
    }
  }
}
